import React from 'react';
import { ChevronDown, Plus, Trash2, X } from 'lucide-react';
import { useWorkspace } from '@/hooks/use-workspace.jsx';
import { cv } from '@/data/cv.js';
export default function Terminal() { const { terminalOpen, toggleTerminal } = useWorkspace(); if (!terminalOpen) return <button className="terminal-reopen" onClick={toggleTerminal}>TERMINAL</button>; return <section className="ide-terminal"><div className="terminal-tabs"><div><button>PROBLEMS</button><button>OUTPUT</button><button className="terminal-tab-active">TERMINAL</button><button>PORTS</button></div><div className="terminal-controls"><span>zsh — {cv.identity.handle}@portfolio</span><Plus size={14}/><Trash2 size={13}/><ChevronDown size={14}/><X size={14} onClick={toggleTerminal}/></div></div><div className="terminal-content"><p><b>{cv.identity.handle}@portfolio</b> <span>~ %</span> whoami</p><p>{cv.identity.role} · {cv.identity.location}</p><p><b>{cv.identity.handle}@portfolio</b> <span>~ %</span> cat skills.ts | head</p><p>PySpark · Python · Scala · SQL · Apache Spark · Airflow · AWS · Databricks</p><p><b>{cv.identity.handle}@portfolio</b> <span>~ %</span> cat availability.json</p><p>{'{ "status": "open", "for": "data engineering roles & collaboration" }'}</p><p><b>{cv.identity.handle}@portfolio</b> <span>~ %</span> <i /></p></div></section>; }
